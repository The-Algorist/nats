# Use a lightweight web server like nginx or http-server
FROM httpd:alpine

# Copy the publisher HTML file to the appropriate directory
COPY index.html /usr/local/apache2/htdocs/

# Expose the port the server runs on
EXPOSE 80
